# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next
/out
# Anchored to the repo root: Next.js never outputs to ./build, and the configurator uses
# /build and /api/build route namespaces (see docs/PHASE-2-BUILD-CONFIGURATOR.md).
/build

# production env / secrets
.env.local
.env*.local

# misc
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor
.vscode
.idea

# raw client photo uploads kept locally, not committed (optimized versions live in public/assets/)
/*.PNG
/*.png
/*.jpg
/*.jpeg

# local Claude settings (machine-specific)
.claude/settings.local.json
